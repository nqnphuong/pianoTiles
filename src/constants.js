export const GAME_WIDTH = window.innerHeight / 2;
export const GAME_HEIGHT = window.innerHeight;

export const LINE_WIDTH = 2;

export const NOTE_WIDTH = GAME_WIDTH / 4 - LINE_WIDTH * 2;
export const NOTE_HEIGHT = (NOTE_WIDTH * 3) / 1.8;

export const SCORE_SIZE = GAME_WIDTH / 9;
export const TITLE_SIZE = GAME_WIDTH / 16;

export const DISTANCE_PRICE = 15;

export const NUMBER_OF_NOTES = 4;

export const POSY_APPEAR_NOTE = - 10;

export const LIST_MUSIC = [
    'HowYouLikeThat_BlackPink'
]
